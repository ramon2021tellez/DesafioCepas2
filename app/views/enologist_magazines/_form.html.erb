<%= form_with(model: enologist_magazine) do |form| %>
  <% if enologist_magazine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enologist_magazine.errors.count, "error") %> prohibited this enologist_magazine from being saved:</h2>

      <ul>
        <% enologist_magazine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
